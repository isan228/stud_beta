# Чек-лист решения проблемы 403 Forbidden

## ✅ Проверено

- [x] Приватный ключ существует и в правильном формате
- [x] Публичный ключ существует
- [x] Ключи соответствуют друг другу (проверено через `diff`)
- [x] API Key установлен в `.env`
- [x] Account ID установлен в `.env`
- [x] Окружение настроено (beta)

## ❓ Что проверить дальше

### 1. Публичный ключ отправлен в Finik?

**Критически важно!** Finik должен иметь ваш публичный ключ для проверки подписи.

**Проверка:**
```bash
# На сервере
cat finik_public.pem
```

**Действие:**
- Скопируйте содержимое `finik_public.pem`
- Отправьте его представителям Finik через безопасный канал (email Finik)
- Убедитесь, что они подтвердили получение и активацию ключа

### 2. API Key активирован в Finik?

**Проверка:**
- Войдите в панель управления Finik
- Проверьте статус вашего API Key
- Убедитесь, что ключ активен и не заблокирован

### 3. Account ID правильный?

**Проверка:**
```bash
cat .env | grep FINIK_ACCOUNT_ID
```

**Действие:**
- Убедитесь, что Account ID соответствует вашему счету в Finik
- Проверьте в панели Finik правильность Account ID

### 4. Окружение соответствует ключам?

**Проверка:**
```bash
cat .env | grep FINIK_ENV
```

**Важно:**
- Если используете beta окружение, убедитесь, что:
  - API Key для beta
  - Account ID для beta
  - Публичный ключ отправлен для beta окружения

### 5. Проверка канонической строки

После обновления кода в логах будет видна каноническая строка для подписи.

**Проверка:**
```bash
pm2 logs stud-kg --lines 100 | grep -A 10 "Canonical string"
```

Убедитесь, что:
- Host заголовок точно совпадает с URL
- Все заголовки x-api-* присутствуют
- Body правильно отсортирован

## Наиболее вероятная причина

**Публичный ключ не отправлен в Finik или отправлен неправильный ключ.**

Finik не может проверить вашу подпись, потому что у них нет вашего публичного ключа.

## Решение

1. **Отправьте публичный ключ в Finik:**
   ```bash
   # На сервере
   cat finik_public.pem
   ```
   Скопируйте весь вывод (включая `-----BEGIN PUBLIC KEY-----` и `-----END PUBLIC KEY-----`)

2. **Отправьте через email Finik** (безопасный канал)

3. **Дождитесь подтверждения** от Finik, что ключ активирован

4. **Попробуйте снова** создать платеж

## Альтернативные причины

Если публичный ключ отправлен, но ошибка сохраняется:

1. **API Key неверный** - свяжитесь с Finik для проверки
2. **Account ID неверный** - проверьте в панели Finik
3. **Проблема с форматом подписи** - проверьте каноническую строку в логах
4. **Окружение не соответствует** - убедитесь, что beta/prod правильно настроены

## Контакты с поддержкой Finik

При обращении в поддержку предоставьте:
1. Логи с канонической строкой (из `📝 Canonical string`)
2. Account ID
3. API Key (первые 10 символов)
4. Окружение (beta/prod)
5. Пример запроса (без реальных ключей)

