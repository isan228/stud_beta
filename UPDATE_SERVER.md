# Инструкция по обновлению сервера

## Шаги для обновления

1. **Проверьте, что код обновлен:**
```bash
cd ~/stud_beta
git pull origin main
```

2. **Проверьте, что файл routes/tests.js содержит изменения:**
```bash
grep -n "process.stdout.write" routes/tests.js
```
Должна быть строка с `process.stdout.write`.

3. **Перезапустите сервер:**
```bash
pm2 restart stud-kg
```

4. **Проверьте статус:**
```bash
pm2 status stud-kg
```

5. **Очистите логи (опционально):**
```bash
pm2 flush stud-kg
```

6. **Пройдите тест в браузере:**
   - Откройте сайт
   - Войдите в систему
   - Выберите тест
   - Пройдите тест (ответьте на вопросы)
   - Завершите тест

7. **Проверьте логи:**
```bash
pm2 logs stud-kg --lines 500 --nostream | grep -E "POST /tests|Checking question|КРИТИЧЕСКАЯ ОШИБКА|Found correct answer|📥|🔍"
```

Или просто:
```bash
pm2 logs stud-kg --lines 500 --nostream
```

## Что искать в логах

- `📥 POST /tests/:testId/check - Проверка ответов` - начало проверки
- `🔍 Checking question X (ДО нормализации)` - проверка каждого вопроса
- `✅ Found correct answer` - правильный ответ найден
- `❌ КРИТИЧЕСКАЯ ОШИБКА: No correct answer found` - правильный ответ НЕ найден

## Если логов нет

Если вы не видите логов с эмодзи, проверьте:
1. Сервер перезапущен: `pm2 restart stud-kg`
2. Код обновлен: `git pull origin main`
3. Файл содержит изменения: `grep "process.stdout.write" routes/tests.js`

