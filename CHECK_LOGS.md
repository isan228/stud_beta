# Как проверить логи сервера для отладки isCorrect

## Шаги для проверки

### 1. Обновите код на сервере

```bash
ssh root@stud.kg
cd /path/to/stud_beta  # Замените на реальный путь
git pull origin main
npm install --production  # Если были новые зависимости
pm2 restart stud-kg
```

### 2. Очистите логи (опционально)

```bash
pm2 flush stud-kg
```

### 3. Пройдите тест в браузере

1. Откройте сайт в браузере
2. Войдите в систему
3. Выберите тест
4. Пройдите тест (ответьте на вопросы)
5. Завершите тест

### 4. Проверьте логи на сервере

```bash
# Последние 200 строк логов
pm2 logs stud-kg --lines 200 --nostream

# Или только ошибки
pm2 logs stud-kg --err --lines 200 --nostream

# Или поиск по ключевым словам
pm2 logs stud-kg --lines 500 --nostream | grep -E "POST /tests|Checking question|КРИТИЧЕСКАЯ ОШИБКА|Found correct answer|📥"
```

### 5. Что искать в логах

Ищите следующие строки:
- `📥 POST /tests/:testId/check - Проверка ответов` - начало проверки
- `🔍 Checking question X (ДО нормализации)` - проверка каждого вопроса
- `✅ Found correct answer` - правильный ответ найден
- `❌ КРИТИЧЕСКАЯ ОШИБКА: No correct answer found` - правильный ответ НЕ найден
- `📊 Comparison for question X` - сравнение ответов

### 6. Если логов нет

Если вы не видите логов с эмодзи (📥, 🔍, ✅, ❌), возможно:
1. Сервер не перезапущен после обновления кода
2. Код не обновлен на сервере
3. console.log не работает (проверьте NODE_ENV)

Проверьте:
```bash
# Проверьте, что код обновлен
cd /path/to/stud_beta
git log --oneline -5

# Проверьте NODE_ENV
pm2 env stud-kg | grep NODE_ENV

# Перезапустите сервер
pm2 restart stud-kg
```

### 7. Альтернативный способ - прямой просмотр файлов логов

```bash
# Все логи
tail -500 /root/.pm2/logs/stud-kg-out.log

# Только ошибки
tail -500 /root/.pm2/logs/stud-kg-error.log

# Поиск в логах
grep "Checking question" /root/.pm2/logs/stud-kg-out.log
grep "КРИТИЧЕСКАЯ ОШИБКА" /root/.pm2/logs/stud-kg-error.log
```

## Важно

После каждого обновления кода нужно:
1. `git pull origin main` - получить изменения
2. `npm install --production` - установить зависимости (если нужно)
3. `pm2 restart stud-kg` - перезапустить сервер

Без перезапуска PM2 изменения не применятся!

