# Тестирование интеграции Finik Payments

## Подготовка к тестированию

### 1. Проверка конфигурации

Убедитесь, что в `.env` настроены все необходимые переменные:

```bash
# На сервере
cd ~/stud_beta
cat .env | grep FINIK
```

Должны быть установлены:
- `FINIK_ENV=beta` (или `prod`)
- `FINIK_API_KEY=your_api_key`
- `FINIK_ACCOUNT_ID=your_account_id`
- `FINIK_WEBHOOK_URL=https://stud.kg/api/payments/webhook`
- `FINIK_REDIRECT_URL=https://stud.kg/payment/success`

### 2. Проверка ключей

```bash
# Проверьте наличие файлов ключей
ls -la finik_private.pem finik_public.pem

# Проверьте права доступа (приватный ключ должен быть защищен)
chmod 600 finik_private.pem
```

### 3. Проверка установки зависимостей

```bash
npm list @mancho.devs/authorizer
# Должна быть версия 2.12.8 или выше
```

## Тестирование через веб-интерфейс

### Шаг 1: Откройте тестовую страницу оплаты

1. Откройте в браузере: `https://stud.kg/payment`
2. Войдите в систему (если не авторизованы)
3. Заполните форму:
   - Сумма: например, 100 сом
   - Описание: "Тестовый платеж"
   - Тип платежа: выберите любой

### Шаг 2: Создание платежа

1. Нажмите "Перейти к оплате"
2. Должен произойти редирект на страницу оплаты Finik
3. URL должен быть вида: `https://qr.finik/...`

### Шаг 3: Проверка в логах

```bash
# Проверьте логи сервера
pm2 logs stud-kg --lines 50

# Должны увидеть:
# ✅ Приватный ключ загружен из файла: finik_private.pem
# Payment created: <payment-id> for user <user-id>
```

## Тестирование через API (curl)

### 1. Получите токен авторизации

```bash
# Войдите через веб-интерфейс и скопируйте токен из localStorage
# Или используйте API:
curl -X POST https://stud.kg/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"identifier":"your_email","password":"your_password"}'
```

### 2. Создайте платеж через API

```bash
TOKEN="your_jwt_token_here"

curl -X POST https://stud.kg/api/payments/create \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "amount": 100,
    "description": "Тестовый платеж через API",
    "paymentType": "test"
  }'
```

Ожидаемый ответ:
```json
{
  "success": true,
  "message": "Платеж создан успешно",
  "paymentId": "uuid-here",
  "paymentUrl": "https://qr.finik/...",
  "transactionId": 1,
  "amount": 100
}
```

## Проверка транзакций в базе данных

### Через psql

```bash
# Подключитесь к БД
psql -U your_db_user -d stud_kg

# Проверьте транзакции
SELECT id, "finikTransactionId", amount, status, "createdAt" 
FROM "Transactions" 
ORDER BY "createdAt" DESC 
LIMIT 10;

# Проверьте транзакции конкретного пользователя
SELECT * FROM "Transactions" WHERE "userId" = 1;
```

### Через API

```bash
# Получите список транзакций
curl -X GET https://stud.kg/api/payments/transactions \
  -H "Authorization: Bearer $TOKEN"
```

## Тестирование Webhook

### 1. Проверка доступности webhook

```bash
# Проверьте, что webhook доступен
curl -X POST https://stud.kg/api/payments/webhook \
  -H "Content-Type: application/json" \
  -d '{"test": "data"}'

# Должен вернуть ошибку валидации (это нормально)
```

### 2. Симуляция webhook от Finik

Для тестирования можно использовать ngrok или другой туннель:

```bash
# Установите ngrok (если нужно)
# ngrok http 3000

# В панели Finik временно укажите webhook URL через ngrok
# После тестирования верните обратно на stud.kg
```

### 3. Проверка логов webhook

```bash
# Следите за логами в реальном времени
pm2 logs stud-kg --lines 0

# После реального платежа должны увидеть:
# ✅ Finik webhook received and validated: { transactionId: ..., status: ..., amount: ... }
# 📝 Transaction X updated to status: SUCCEEDED
```

## Проверка после реального платежа

### 1. Проверка статуса транзакции

```bash
# Через API
curl -X GET https://stud.kg/api/payments/transactions/1 \
  -H "Authorization: Bearer $TOKEN"

# Через БД
SELECT status, "transactionDate", "receiptNumber" 
FROM "Transactions" 
WHERE id = 1;
```

### 2. Проверка обработки успешного платежа

В логах должны быть сообщения:
- `✅ Processing successful payment for user X`
- `📝 Transaction X updated to status: SUCCEEDED`

## Типичные проблемы и решения

### Проблема: "Приватный ключ не найден"

**Решение:**
```bash
# Убедитесь, что файл существует
ls -la finik_private.pem

# Проверьте права доступа
chmod 600 finik_private.pem

# Или установите переменную в .env
# FINIK_PRIVATE_KEY_PEM=...
```

### Проблема: "Invalid signature" от Finik API

**Решение:**
1. Проверьте, что публичный ключ отправлен в Finik
2. Убедитесь, что используется правильное окружение (beta/prod)
3. Проверьте формат приватного ключа

### Проблема: Webhook не приходит

**Решение:**
1. Проверьте доступность URL: `curl https://stud.kg/api/payments/webhook`
2. Убедитесь, что URL указан в панели Finik
3. Проверьте файрвол и Nginx конфигурацию

### Проблема: Редирект не работает

**Решение:**
1. Проверьте, что `redirect: 'manual'` установлен в fetch
2. Проверьте логи - должен быть статус 302
3. Убедитесь, что `paymentUrl` возвращается в ответе

## Чек-лист тестирования

- [ ] Конфигурация `.env` настроена
- [ ] Ключи `finik_private.pem` и `finik_public.pem` существуют
- [ ] Зависимости установлены (`npm install`)
- [ ] Сервер запущен (`pm2 status`)
- [ ] Тестовая страница открывается (`/payment`)
- [ ] Платеж создается успешно
- [ ] Редирект на Finik работает
- [ ] Транзакция сохраняется в БД
- [ ] Webhook обрабатывается (после реального платежа)
- [ ] Статус транзакции обновляется

## Полезные команды

```bash
# Проверка статуса сервера
pm2 status stud-kg

# Просмотр логов
pm2 logs stud-kg --lines 100

# Перезапуск сервера
pm2 restart stud-kg

# Проверка подключения к БД
psql -U your_db_user -d stud_kg -c "SELECT COUNT(*) FROM \"Transactions\";"

# Проверка последних транзакций
psql -U your_db_user -d stud_kg -c "SELECT id, amount, status, \"createdAt\" FROM \"Transactions\" ORDER BY \"createdAt\" DESC LIMIT 5;"
```

