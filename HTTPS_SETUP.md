# Настройка HTTPS для stud.kg

## Проблема: HTTP работает, HTTPS не работает

Если сайт работает по HTTP, но не работает по HTTPS, это означает, что SSL сертификат не настроен или настроен неправильно.

## Быстрое решение (автоматическая настройка)

```bash
# Сделайте скрипт исполняемым
chmod +x scripts/setupSSL.sh

# Запустите с правами sudo
sudo npm run setup-ssl
```

Скрипт автоматически:
1. Установит Certbot (если не установлен)
2. Создаст конфигурацию Nginx
3. Получит SSL сертификат от Let's Encrypt
4. Настроит автоматическое обновление

## Ручная настройка

### Шаг 1: Установка Certbot

**Ubuntu/Debian:**
```bash
sudo apt-get update
sudo apt-get install certbot python3-certbot-nginx
```

**CentOS/RHEL:**
```bash
sudo yum install certbot python3-certbot-nginx
```

### Шаг 2: Получение SSL сертификата

```bash
sudo certbot --nginx -d stud.kg -d www.stud.kg
```

Certbot автоматически:
- Получит сертификат от Let's Encrypt
- Обновит конфигурацию Nginx
- Настроит редирект с HTTP на HTTPS

### Шаг 3: Проверка конфигурации

```bash
# Проверка конфигурации
sudo nginx -t

# Перезагрузка Nginx
sudo systemctl reload nginx
```

### Шаг 4: Проверка работы

Откройте в браузере:
- https://stud.kg
- https://www.stud.kg

## Ручная настройка конфигурации Nginx

Если автоматическая настройка не сработала, создайте конфигурацию вручную:

```bash
sudo nano /etc/nginx/sites-available/stud-kg
```

Используйте пример из файла `nginx-https.conf.example`:

```nginx
server {
    listen 80;
    server_name stud.kg www.stud.kg;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name stud.kg www.stud.kg;

    ssl_certificate /etc/letsencrypt/live/stud.kg/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/stud.kg/privkey.pem;
    
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
```

**Важно:** Замените `stud.kg` на ваш домен и проверьте пути к сертификатам.

## Проверка сертификата

```bash
# Проверка сертификата
sudo certbot certificates

# Тест обновления (не обновляет реально)
sudo certbot renew --dry-run
```

## Автоматическое обновление

Certbot автоматически обновляет сертификаты, но можно добавить в cron:

```bash
# Добавить в crontab
sudo crontab -e

# Добавить строку (проверка каждый день в 2:00)
0 2 * * * certbot renew --quiet
```

## Решение проблем

### Проблема: "Failed to obtain certificate"

**Причины:**
1. Домен не указывает на ваш сервер
2. Порт 80 заблокирован файрволом
3. Nginx не запущен

**Решение:**
```bash
# Проверьте DNS
nslookup stud.kg

# Проверьте файрвол
sudo ufw status
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp

# Проверьте Nginx
sudo systemctl status nginx
```

### Проблема: "Certificate not found"

**Решение:**
```bash
# Проверьте наличие сертификата
sudo ls -la /etc/letsencrypt/live/stud.kg/

# Если нет, получите заново
sudo certbot --nginx -d stud.kg -d www.stud.kg
```

### Проблема: "502 Bad Gateway" после настройки HTTPS

**Причина:** Приложение не запущено или неправильный proxy_pass

**Решение:**
```bash
# Проверьте, запущено ли приложение
pm2 status

# Если нет, запустите
pm2 start server.js --name stud-kg

# Проверьте конфигурацию Nginx
sudo nginx -t
```

### Проблема: Смешанный контент (Mixed Content)

Если браузер показывает предупреждение о смешанном контенте, убедитесь, что:
1. Все ссылки в коде используют HTTPS или относительные пути
2. В `server.js` нет жестко заданных HTTP ссылок

## Проверка безопасности SSL

Проверьте ваш SSL на:
- https://www.ssllabs.com/ssltest/
- https://securityheaders.com/

## Важные моменты

1. **Let's Encrypt сертификаты действительны 90 дней** - они обновляются автоматически
2. **Порт 80 должен быть открыт** для проверки домена
3. **DNS должен указывать на ваш сервер** перед получением сертификата
4. **Не забудьте обновить конфигурацию** если меняете домен

## После настройки

После успешной настройки HTTPS:
1. Сайт будет автоматически редиректить HTTP на HTTPS
2. Сертификат будет обновляться автоматически
3. Браузеры будут показывать зеленый замочек 🔒

